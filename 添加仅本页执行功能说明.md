# 添加"仅本页"执行功能说明

## 问题描述
之前点击RUN运行时，会处理所有图片，即使只想对当前页面重新执行OCR/翻译也不行。这在调整参数后想测试单页效果时非常不方便。

## 解决方案
在左侧栏的设置按钮上方添加"仅本页"按钮，点击后只对当前选中的页面执行OCR/翻译。

## 修改的文件

### 1. BallonsTranslator/ui/mainwindowbars.py

**添加内容**：
- 在LeftBar类中添加"仅本页"按钮
- 按钮位置：在configChecker（设置按钮）上方

```python
# 添加"仅本页"按钮
self.runCurrentPageBtn = QPushButton()
self.runCurrentPageBtn.setObjectName('RunCurrentPageButton')
self.runCurrentPageBtn.setText('仅本页')
self.runCurrentPageBtn.setFont(font)
self.runCurrentPageBtn.setFixedSize(LEFTBTN_WIDTH, LEFTBTN_WIDTH)
self.run_current_page_clicked = self.runCurrentPageBtn.clicked

# 布局顺序（从上到下）：
# - 其他按钮...
# - 弹性空间
# - 仅本页按钮 ← 新增
# - 设置按钮
# - Run按钮
```

### 2. BallonsTranslator/ui/mainwindow.py

**添加内容**：

#### 2.1 连接信号
```python
self.leftBar.run_current_page_clicked.connect(self.run_current_page)
```

#### 2.2 添加run_current_page方法
```python
def run_current_page(self):
    """仅对当前页面执行OCR/翻译"""
    if self.imgtrans_proj.is_empty:
        return
    
    current_page = self.imgtrans_proj.current_img
    if not current_page:
        return
    
    # 只处理当前页面
    pages_to_process = [current_page]
    self.on_run_imgtrans(continue_mode=False, pages_to_process=pages_to_process)
```

#### 2.3 修改on_run_imgtrans方法
- 添加`pages_to_process`参数
- 支持直接指定要处理的页面列表

```python
def on_run_imgtrans(self, continue_mode=False, pages_to_process=None):
    # 如果没有指定pages_to_process，则根据continue_mode决定
    if pages_to_process is None:
        pages_to_process = []
    
    # 继续模式：先检查哪些页面需要处理
    if continue_mode and not pages_to_process:
        # 原有的继续模式逻辑...
```

## 功能特点

### 1. 按钮位置
```
┌─────────────┐
│   Open      │
│   PageList  │
│   Search    │
│   ImgTrans  │
│             │
│   (空间)    │
│             │
│   仅本页    │ ← 新增
│   设置      │
│   Run       │
└─────────────┘
```

### 2. 使用场景

#### 场景1：调整参数后测试
1. 调整OCR或翻译参数
2. 点击"仅本页"按钮
3. 只对当前页面重新执行
4. 查看效果，满意后再对所有页面执行

#### 场景2：修复单页问题
1. 发现某一页识别不准确
2. 切换到该页面
3. 调整参数
4. 点击"仅本页"重新执行
5. 不影响其他页面

#### 场景3：快速测试
1. 打开新项目
2. 选择一张代表性的图片
3. 点击"仅本页"快速测试
4. 确认参数合适后再全部运行

### 3. 工作原理

1. **获取当前页面**
   - 从`imgtrans_proj.current_img`获取当前选中的页面

2. **创建页面列表**
   - 创建只包含当前页面的列表：`[current_page]`

3. **调用处理方法**
   - 调用`on_run_imgtrans(pages_to_process=[current_page])`
   - 传递单页列表给处理流程

4. **处理流程**
   - `module_manager`接收页面列表
   - `ImgtransThread`只处理列表中的页面
   - 其他页面被跳过

## 与其他功能的关系

### 与"继续运行"的区别
- **继续运行**：处理所有未完成的页面
- **仅本页**：只处理当前选中的页面

### 与"Run"的区别
- **Run**：处理所有页面（或继续运行未完成的）
- **仅本页**：只处理当前页面

### 优先级
```
仅本页 > 继续运行 > 全部运行
```

## 使用效果

### 优势
- ✅ 快速测试参数效果
- ✅ 节省时间，不需要处理所有页面
- ✅ 方便调试和优化
- ✅ 不影响其他页面的结果

### 注意事项
1. **当前页面**：确保在页面列表中选中了要处理的页面
2. **覆盖结果**：会覆盖当前页面的现有结果
3. **不弹确认框**：直接执行，不会询问是否覆盖

## 测试建议

1. 打开包含多张图片的项目
2. 选择其中一张图片
3. 点击"仅本页"按钮
4. 观察是否只处理了当前页面
5. 切换到其他页面，确认未被处理
6. 调整参数后再次点击"仅本页"
7. 确认可以重复执行

## 界面效果

左侧栏按钮布局（从上到下）：
1. Open（打开项目）
2. PageList（页面列表）
3. GlobalSearch（全局搜索）
4. ImgTrans（图像翻译）
5. **[弹性空间]**
6. **仅本页** ← 新增
7. 设置
8. Run

按钮样式与Run按钮一致，文字为"仅本页"。
