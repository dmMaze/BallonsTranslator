# 区域合并性能优化说明

## 问题描述

当对大量文件（如595页）执行区域合并时，界面会卡住无响应，因为合并任务在主线程中同步执行。

## 解决方案

将区域合并任务移到后台线程执行，并添加进度显示和停止功能。

## 修改的文件

### 1. BallonsTranslator/ui/io_thread.py

**新增 MergeThread 类**：
```python
class MergeThread(ThreadBase):
    """区域合并后台线程"""
    progress_changed = Signal(int, int)  # (当前进度, 总数)
    merge_finished = Signal(int, int)  # (成功数, 失败数)
    
    def runMerge(self, json_path, img_list, config):
        """启动合并任务"""
        
    def requestStop(self):
        """请求停止"""
        
    def _run_merge(self):
        """执行合并任务"""
```

### 2. BallonsTranslator/ui/mainwindow.py

**修改 run_merge_task 方法**：
- 对所有文件运行时，使用后台线程

**新增方法**：
- `run_merge_all_async()` - 异步执行合并
- `on_merge_progress()` - 进度更新回调
- `on_merge_stop()` - 停止回调
- `on_merge_finished()` - 完成回调

### 3. BallonsTranslator/ui/custom_widget/message.py

**修改 ProgressMessageBox 类**：
- 添加 `stop_clicked` 信号
- 添加停止按钮
- 添加 `zero_progress()` 方法

## 功能特点

### 1. 后台执行
- 合并任务在后台线程中执行
- 主界面保持响应，不会卡住

### 2. 进度显示
- 显示当前进度（如 "50/595"）
- 进度条实时更新

### 3. 停止功能
- 可以随时点击"停止"按钮中断合并
- 已完成的合并结果会保留

### 4. 完成通知
- 合并完成后显示结果统计
- 自动刷新界面显示

## 使用方法

1. 打开区域合并工具（工具 → 区域合并工具）
2. 设置合并参数
3. 点击"对所有文件运行"
4. 进度对话框显示，可以看到进度
5. 如需停止，点击"停止"按钮
6. 完成后显示结果统计

## 技术实现

### 线程通信
```
主线程                    后台线程
   |                         |
   |-- runMerge() --------->|
   |                         |-- 处理文件1
   |<-- progress_changed ---|
   |                         |-- 处理文件2
   |<-- progress_changed ---|
   |                         |-- ...
   |<-- merge_finished -----|
   |                         |
```

### 停止机制
```python
def requestStop(self):
    self.stop_requested = True

def _run_merge(self):
    for img_name in self.img_list:
        if self.stop_requested:
            break
        # 处理文件...
```

## 性能对比

### 优化前
- 595页合并：界面卡死约30秒
- 无法中断
- 无进度显示

### 优化后
- 595页合并：界面保持响应
- 可随时停止
- 实时进度显示

## 注意事项

1. **停止后的数据**：停止时已完成的合并会保留
2. **重新运行**：可以多次运行合并，会覆盖之前的结果
3. **单文件运行**：单文件运行仍然是同步的（因为很快）

## 测试建议

1. 打开包含大量图片的项目（如595页）
2. 打开区域合并工具
3. 点击"对所有文件运行"
4. 观察：
   - 界面是否保持响应
   - 进度是否正确显示
   - 停止按钮是否有效
5. 等待完成，检查结果统计
