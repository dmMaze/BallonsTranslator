# تقرير تحليل مشروع BallonsTranslator

### 1. تحليل البنية العامة للمشروع
يعتمد المشروع على بنية **نمطية (Modular)** احترافية مكتوبة بلغة **Python**، ويستخدم إطار عمل **Qt** للواجهة الرسومية.
*   **المرونة:** يسمح نظام السجل (Registry) بإضافة محركات جديدة بسهولة.
*   **الأداء:** استخدام خيوط المعالجة الخلفية يضمن استجابة الواجهة.

### 2. تحليل الأنظمة الفرعية

#### نظام OCR
*   دعم واسع لمحركات مثل `manga-ocr` و `PaddleOCR`.
*   دقة عالية في اكتشاف النصوص باستخدام `YOLOv5`.

#### نظام الترجمة
*   تكامل مع النماذج اللغوية الكبيرة (GPT-4, Sakura LLM).
*   دعم المحركات التقليدية (DeepL, Google).

#### نظام الترميم (Inpainting) - التركيز الأساسي
*   **Lama:** المحرك الأقوى لترميم خلفيات المانجا المنسوجة.
*   **Flux.2 Klein:** جودة فائقة تعتمد على نماذج الانتشار (Diffusion).
*   **الذكاء:** توسيع القناع تلقائياً لضمان الاندماج المثالي.
*   **تنبيه:** يتم تشغيل هذه النماذج داخل واجهة البرنامج نفسه وليس كإضافات مباشرة داخل برامج أخرى.

#### واجهة المستخدم
*   تحرير مباشر (WYSIWYG) مع تحكم دقيق في التنسيق والطبقات.

### 3. نقاط القوة
*   تعدد المحركات والخيارات.
*   جودة ترميم استثنائية باستخدام ذكاء اصطناعي متخصص.
*   تصدير البيانات لبرنامج Photoshop (نصوص وطبقات).

### 4. التوضيح بشأن "التكامل مع Photoshop"
التكامل الحالي **ليس إضافة (Plugin) تعمل داخل فوتوشوب** للقيام بالترميم، بل هو عبارة عن **سكريبت (Script)** بصيغة `.jsx` يقوم بـ:
1.  تصدير العمل من BallonsTranslator بصيغة JSON.
2.  استيراد هذا الملف داخل Photoshop لإنشاء طبقات نصية مطابقة لما تم ترجمته في البرنامج.
3.  **بالنسبة للترميم (LAMA/Flux):** يجب القيام به داخل BallonsTranslator أولاً، ثم تصدير الصورة النظيفة إلى فوتوشوب لإكمال العمل الفني إذا رغبت في ذلك.

### 5. نقاط الضعف
*   استهلاك عالٍ للموارد (VRAM).
*   تعقيد التثبيت الأولي.

### 6. اقتراحات التحسين
*   دمج نموذج **SAM** لتحسين دقة التحديد.
*   نظام توليد خيارات متعددة للترميم.

### 7. أفكار تطوير جديدة
*   ترجمة المؤثرات البصرية (SFX) آلياً.
*   وضع التنظيف الكامل (Batch Cleaning).

### 8. ميزة جديدة: واجهة برمجة التطبيقات (API) للدمج مع Photoshop
تمت إضافة دعم تشغيل المشروع كخادم (API Server) باستخدام FastAPI. تتيح هذه الميزة للمطورين بناء إضافات لبرامج خارجية (مثل Photoshop UXP) تتصل بـ BallonsTranslator كـ "محرك ذكاء اصطناعي" خلفي.

**المميزات الجديدة:**
*   تشغيل البرنامج عبر الأمر: `python launch.py --api`.
*   نقاط نهاية (Endpoints) لـ:
    *   `POST /ocr`: لاكتشاف النصوص واستخراجها من الصور.
    *   `POST /translate`: لترجمة النصوص باستخدام أي من المحركات المدعومة.
    *   `POST /inpaint`: لإزالة النصوص وترميم الخلفية (يدعم Lama و Flux).
*   دعم كامل لتبادل الصور بصيغة Base64.
*   توثيق كامل للمطورين في ملف `DOC_API.md`.
