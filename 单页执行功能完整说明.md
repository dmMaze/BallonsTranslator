# 单页执行功能完整说明

## 功能概述

在左侧栏添加"本"按钮，点击后只对当前选中的页面执行OCR/翻译，而不是处理所有页面。

## 问题背景

### 原有问题
- 点击RUN运行时，会处理所有图片（例如595页）
- 调整参数后想测试单页效果，必须等待所有页面处理完成
- 无法快速验证参数设置是否合适
- 对于大型项目（几百页）非常不方便

### 解决方案
添加"本"按钮，实现单页快速执行功能。

## 界面变化

### 按钮位置
```
左侧栏布局（从上到下）：
┌─────────────┐
│   Open      │  打开项目
│   PageList  │  页面列表
│   Search    │  全局搜索
│   ImgTrans  │  图像翻译
│             │
│   (空间)    │
│             │
│   本        │  ← 新增：仅本页执行
│   设置      │  配置
│   Run       │  全部运行
└─────────────┘
```

### 按钮样式
- **文字**：本
- **提示**：仅对本页执行
- **大小**：与Run按钮相同
- **位置**：设置按钮上方

## 使用方法

### 基本使用
1. 在页面列表中选择要处理的页面
2. 点击"本"按钮
3. 只处理当前选中的页面
4. 处理完成后停留在当前页面

### 典型场景

#### 场景1：参数调试
```
问题：不确定OCR参数是否合适
步骤：
1. 选择一张代表性的图片
2. 调整OCR参数
3. 点击"本"按钮测试
4. 查看效果
5. 满意后再用Run处理所有页面
```

#### 场景2：单页修复
```
问题：某一页识别效果不好
步骤：
1. 切换到问题页面
2. 调整相关参数
3. 点击"本"按钮重新处理
4. 不影响其他已处理的页面
```

#### 场景3：快速验证
```
问题：新项目不知道参数如何设置
步骤：
1. 打开项目
2. 选择第一张图片
3. 点击"本"按钮快速测试
4. 根据结果调整参数
5. 确认后再全部运行
```

## 技术实现

### 修改的文件

#### 1. ui/mainwindowbars.py
添加"本"按钮到左侧栏

```python
# 添加"本"按钮（仅本页）
self.runCurrentPageBtn = QPushButton()
self.runCurrentPageBtn.setObjectName('RunCurrentPageButton')
self.runCurrentPageBtn.setText('本')
self.runCurrentPageBtn.setToolTip('仅对本页执行')
self.runCurrentPageBtn.setFont(font)
self.runCurrentPageBtn.setFixedSize(LEFTBTN_WIDTH, LEFTBTN_WIDTH)
self.run_current_page_clicked = self.runCurrentPageBtn.clicked
```

#### 2. ui/mainwindow.py

**添加run_current_page方法**：
```python
def run_current_page(self):
    """仅对当前页面执行OCR/翻译"""
    if self.imgtrans_proj.is_empty:
        return
    
    current_page = self.imgtrans_proj.current_img
    if not current_page:
        QMessageBox.warning(self, '警告', '没有选中的页面')
        return
    
    # 保存当前页面索引，执行完成后不要跳转
    self.run_single_page_mode = True
    self.single_page_index = self.pageList.currentIndex().row()
    
    # 只处理当前页面
    pages_to_process = [current_page]
    self.on_run_imgtrans(continue_mode=False, pages_to_process=pages_to_process)
```

**修改on_run_imgtrans方法**：
- 添加`pages_to_process`参数
- 支持指定要处理的页面列表

**修改on_pagtrans_finished方法**：
- 检查是否为单页模式
- 单页模式下不跳转页面

```python
# 如果是单页模式，保持在当前页面不跳转
if hasattr(self, 'run_single_page_mode') and self.run_single_page_mode:
    # 更新当前页面的显示
    self.canvas.updateCanvas()
    self.st_manager.updateSceneTextitems()
else:
    # 正常模式：跳转到完成的页面
    if page_index != self.pageList.currentIndex().row():
        self.pageList.setCurrentRow(page_index)
```

#### 3. ui/module_manager.py

**修改_imgtrans_pipeline方法**：
- 根据`pages_to_process`决定要遍历的页面
- 只处理指定的页面，不遍历所有页面

```python
# 如果指定了pages_to_process，只处理这些页面
if self.pages_to_process is not None:
    pages_to_iterate = self.pages_to_process
    self.num_pages = num_pages = len(self.pages_to_process)
    LOGGER.info(f'Processing specific pages: {pages_to_iterate}')
else:
    pages_to_iterate = list(self.imgtrans_proj.pages.keys())
    self.num_pages = num_pages = len(self.imgtrans_proj.pages)
    LOGGER.info(f'Processing all {num_pages} pages')

for imgname in pages_to_iterate:
    # 处理页面...
```

## 工作流程

### 执行流程
```
1. 用户点击"本"按钮
   ↓
2. 获取当前选中的页面名称
   ↓
3. 设置单页模式标志
   ↓
4. 创建只包含当前页面的列表
   ↓
5. 调用on_run_imgtrans(pages_to_process=[当前页])
   ↓
6. 传递给module_manager
   ↓
7. 只遍历和处理当前页面
   ↓
8. 完成后停留在当前页面
   ↓
9. 重置单页模式标志
```

### 关键点

#### 1. 页面识别
- 从`imgtrans_proj.current_img`获取当前页面名称
- 确保获取的是用户选中的页面

#### 2. 避免遍历所有页面
- 原来：遍历595页，跳过594页
- 现在：只遍历1页
- 性能提升：从O(n)到O(1)

#### 3. 进度显示
- `num_pages = 1`
- 进度条从0%到100%只处理1页
- 不会显示错误的进度

#### 4. 页面不跳转
- 设置`run_single_page_mode`标志
- 在`on_pagtrans_finished`中检查标志
- 单页模式下只更新显示，不跳转

## 与其他功能的对比

### 三种运行模式

| 功能 | 处理范围 | 使用场景 | 速度 |
|------|---------|---------|------|
| **本** | 当前1页 | 快速测试、单页修复 | 最快 |
| **继续运行** | 未完成的页面 | 断点续传 | 中等 |
| **Run** | 所有页面 | 完整处理 | 最慢 |

### 功能对比

#### 本 vs Run
- **本**：只处理当前页，不弹确认框，直接执行
- **Run**：处理所有页，可能弹确认框询问是否继续运行

#### 本 vs 继续运行
- **本**：只处理当前选中的1页
- **继续运行**：处理所有未完成的页面

## 优势与特点

### 优势
✅ **快速测试**：几秒钟就能看到效果  
✅ **节省时间**：不需要等待处理所有页面  
✅ **方便调试**：可以反复调整参数测试  
✅ **不影响其他页**：只处理当前页，其他页保持不变  
✅ **停留当前页**：处理完成后不跳转  

### 特点
- 无确认对话框，直接执行
- 会覆盖当前页的现有结果
- 支持所有处理阶段（检测、OCR、翻译、修复）
- 进度显示准确（0-100%对应1页）

## 注意事项

### 1. 页面选择
- 确保在页面列表中选中了要处理的页面
- 如果没有选中页面，会弹出警告

### 2. 结果覆盖
- 会覆盖当前页面的现有OCR/翻译结果
- 不会弹出确认对话框
- 如果不想覆盖，请先备份

### 3. 参数设置
- 使用当前的OCR/翻译参数
- 建议先在单页测试参数效果
- 满意后再用Run处理所有页面

### 4. 大型项目
- 对于595页这样的大项目特别有用
- 可以先用"本"测试几页
- 确认效果后再全部运行

## 测试建议

### 测试步骤
1. 打开包含多张图片的项目
2. 选择第5张图片
3. 点击"本"按钮
4. 观察是否只处理了第5张
5. 确认处理完成后停留在第5张
6. 切换到其他页面，确认未被处理
7. 调整参数后再次点击"本"
8. 确认可以重复执行

### 预期结果
- ✅ 只处理选中的页面
- ✅ 进度条显示正确（0-100%）
- ✅ 处理速度很快
- ✅ 完成后停留在当前页
- ✅ 其他页面不受影响

## 常见问题

### Q1: 点击"本"后处理的是第一页？
**A**: 这个问题已修复。现在会正确处理当前选中的页面。

### Q2: 处理完成后跳转到第一页？
**A**: 这个问题已修复。现在会停留在当前页面。

### Q3: 处理单页时软件卡死？
**A**: 这个问题已修复。现在只遍历1页，不会遍历所有页面。

### Q4: 进度条显示错误的数字？
**A**: 这个问题已修复。现在`num_pages=1`，进度显示正确。

### Q5: 可以连续处理多个单页吗？
**A**: 可以。选择页面 → 点击"本" → 选择下一页 → 点击"本"。

## 总结

"本"按钮是一个非常实用的功能，特别适合：
- 大型项目（几百页）的参数调试
- 单页问题的快速修复
- 新项目的参数验证

通过只处理当前页面，大大提高了工作效率，让用户可以快速迭代和优化参数设置。
