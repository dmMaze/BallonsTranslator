# BallonsTranslator 功能修改说明

## 📅 修改日期
2024年11月29日

---

## 🎯 本次修改内容总览

本次对 BallonsTranslator 进行了以下修改：
1. ✅ 添加区域合并工具功能
2. ✅ 禁用日志文件自动生成
3. ✅ 优化用户交互体验

---

## 1️⃣ 区域合并工具功能

### 功能描述
从 X-AnyLabeling 项目移植了区域合并工具，可以智能合并相邻的文本框区域，提高标注效率。

### 新增文件

#### `BallonsTranslator/utils/merger.py` (约 400 行)
- 核心合并算法逻辑
- 支持垂直、水平、组合合并模式
- 使用并查集算法识别可合并文本框组
- 支持多种标签过滤和合并策略
- 完全适配 BallonsTranslator 的 JSON 格式（pages 结构、xyxy 字段）

#### `BallonsTranslator/ui/merge_dialog.py` (约 280 行)
- 图形化配置界面对话框
- 包含所有合并参数的设置选项
- 支持当前文件和批量处理两种模式
- 完整的中文界面
- 优化的交互逻辑：执行合并后对话框保持打开，方便调整参数

### 修改文件

#### `BallonsTranslator/ui/mainwindowbars.py`
**修改位置**：`create_tool_bar()` 方法

**添加内容**：
```python
# 在工具菜单中添加区域合并工具
self.merge_tool_act = QAction(self.tr('Region Merge Tool'), self)
self.merge_tool_act.setShortcut('Ctrl+Shift+M')
self.merge_tool_act.triggered.connect(self.on_open_merge_tool)
self.tool_menu.addAction(self.merge_tool_act)
```

#### `BallonsTranslator/ui/mainwindow.py`
**添加方法**：
- `on_open_merge_tool()`：打开合并工具对话框
- `run_merge_task(on_current=False)`：执行合并任务

**功能特点**：
- 支持当前文件和批量处理
- 合并后自动重新加载项目数据
- 自动刷新画布显示
- 详细的错误提示和建议

### 使用方法

#### 打开方式
1. **菜单方式**：点击 `工具` → `区域合并工具`
2. **快捷键**：按 `Ctrl+Shift+M`

#### 主要功能

**合并模式**：
- 垂直合并（上下相邻）
- 水平合并（左右相邻）
- 先垂直后水平
- 先水平后垂直
- 无（不合并）

**文本合并顺序**（按标签）：
- 从左到右 (LTR)：适用于英文等
- 从右到左 (RTL)：适用于日文气泡等（默认：balloon, qipao, shuqing）
- 从上到下 (TTB)：适用于竖排文本（默认：changfangtiao, hengxie）

**标签合并规则**：
- 标签合并策略：优先使用较短的标签 / 使用第一个框的标签 / 组合标签 / 优先使用非默认标签
- 黑名单标签：排除不参与合并的标签（默认：other）
- 要求标签完全相同才合并
- 仅在特定标签组内合并

**几何合并参数**：
- 垂直合并：最大垂直间隙（默认 10px）、最小水平重叠比例（默认 90%）
- 水平合并：最大水平间隙（默认 10px）、最小垂直重叠比例（默认 90%）

**高级选项**：
- 允许负间隙（即允许框本身有重叠）

**合并结果类型**：
- 合并水平矩形
- 合并旋转矩形

#### 操作步骤
1. 打开一个包含 JSON 标注文件的项目
2. 点击菜单 `工具` → `区域合并工具` 或按 `Ctrl+Shift+M`
3. 根据需要调整合并参数
4. 选择执行方式：
   - **对当前文件运行**：只处理当前打开的图片
   - **对所有文件运行**：处理项目中的所有图片
5. 点击相应按钮执行合并
6. 查看合并结果（对话框保持打开，可继续调整参数）
7. 点击"取消"关闭对话框

### 注意事项
⚠️ **重要提醒**：
- 合并操作会直接修改 JSON 文件，建议操作前备份重要数据
- 不同图片可能需要不同参数，建议先在单个文件上测试
- 目前没有内置撤销功能，建议使用版本控制系统（如 Git）

### 界面优化
- ✅ 执行合并后对话框保持打开，方便用户调整参数后再次运行
- ✅ 特定标签组输入框高度增加到 5 行（100-120px），显示更清晰
- ✅ 所有提示信息均为中文

---

## 2️⃣ 禁用日志文件自动生成

### 问题描述
每次启动 BallonsTranslator 都会在 `logs/` 目录下生成一个时间戳命名的日志文件（如 `_2025_11_29-14_28_59.log`），长期使用会积累大量日志文件。

### 修改文件
`BallonsTranslator/utils/logger.py`

### 修改内容
注释掉 `setup_logging()` 函数中的日志文件创建代码，只保留控制台输出。

**修改前**：
- 每次启动生成新的日志文件
- 自动清理超过 14 个的旧日志文件

**修改后**：
- ✅ 不再生成日志文件
- ✅ 日志信息仍然输出到控制台（命令行窗口）
- ✅ 如需调试，可以取消注释重新启用

### 如何重新启用日志文件
如果需要重新启用日志文件功能（用于调试），在 `utils/logger.py` 中取消 `setup_logging()` 函数内的注释即可。

### 清理现有日志文件
可以手动删除 `BallonsTranslator/logs/` 目录下的所有 `.log` 文件。

---

## 📊 修改统计

### 文件统计
- **新增文件**：2 个（merger.py, merge_dialog.py）
- **修改文件**：3 个（mainwindow.py, mainwindowbars.py, logger.py）
- **文档文件**：3 个（使用说明、集成说明、PR说明）

### 代码统计
- **新增代码**：约 680 行
- **修改代码**：约 80 行
- **注释代码**：约 30 行

---

## 🔧 技术实现亮点

### 区域合并工具
1. **格式适配**：完全适配 BallonsTranslator 的 JSON 格式（pages 结构、xyxy 字段）
2. **算法优化**：使用并查集算法高效识别合并组
3. **智能判断**：基于几何位置和标签规则的合并判断
4. **用户体验**：对话框保持打开，方便多次调整参数

### 日志系统
1. **灵活配置**：可以轻松启用/禁用日志文件
2. **保留功能**：控制台输出仍然保留，方便实时查看
3. **易于恢复**：需要时可以快速重新启用

---

## 🚀 后续优化建议

### 区域合并工具
1. **撤销功能**：添加合并操作的撤销机制
2. **预览功能**：在执行前显示合并预览
3. **配置保存**：保存常用的合并参数配置
4. **性能优化**：对大量文本框的处理进行优化
5. **国际化**：支持多语言界面

### 日志系统
1. **配置选项**：在设置界面中添加日志开关
2. **日志级别**：允许用户选择日志级别（DEBUG/INFO/WARNING/ERROR）
3. **日志查看器**：内置日志查看器，方便查看历史日志

---

## 📝 版本信息

**修改版本**：基于原始 BallonsTranslator 项目  
**修改日期**：2024年11月29日  
**修改者**：用户自定义修改  
**功能状态**：✅ 完全可用  
**测试状态**：✅ 已测试通过

---

## 🔗 相关文档

- [区域合并工具使用说明](./区域合并工具使用说明.md) - 详细的用户使用指南
- [区域合并工具集成说明](./区域合并工具集成说明.txt) - 技术实现说明
- [PR提交说明](./PR提交说明.md) - GitHub PR 提交指南

---

## ❓ 常见问题

### Q1: 为什么有些文本框没有被合并？
**A**: 检查以下几点：
- 文本框之间的间隙是否超过了设置的最大间隙
- 文本框的重叠比例是否满足最小重叠比例要求
- 标签是否在黑名单中
- 是否启用了"要求标签完全相同才合并"选项

**建议**：
1. 尝试增大最大间隙值（如 100-200）
2. 降低最小重叠比例（如 50-70%）
3. 取消勾选"启用排除合并的标签"
4. 检查标签配置

### Q2: 合并后的文本顺序不对怎么办？
**A**: 检查文本合并顺序设置，确保标签对应的阅读方向正确。
- 日文气泡通常使用 RTL（从右到左）
- 英文文本使用 LTR（从左到右）
- 竖排文本使用 TTB（从上到下）

### Q3: 如何撤销合并操作？
**A**: 目前没有内置的撤销功能，建议：
- 使用版本控制系统（如 Git）
- 在操作前手动备份 JSON 文件
- 先在单个文件上测试，确认效果后再批量处理

### Q4: 为什么执行 OCR 时报错"另一个程序正在使用此文件"？
**A**: 这是 Windows 文件权限问题，不是修改导致的。解决方法：
1. 关闭所有可能打开该 JSON 文件的程序
2. 暂停云同步软件（OneDrive、百度云盘等）
3. 重启 BallonsTranslator 程序
4. 如果还是不行，重启电脑

### Q5: 如何重新启用日志文件？
**A**: 编辑 `utils/logger.py` 文件，在 `setup_logging()` 函数中取消注释即可。

---

## 📧 反馈与支持

如有问题或建议，请：
1. 查看相关文档
2. 检查常见问题
3. 在项目 GitHub 页面提交 Issue
4. 联系项目维护者

---

**文档版本**：v1.0  
**最后更新**：2024年11月29日
