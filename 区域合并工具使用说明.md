# 区域合并工具使用说明

## 功能简介

区域合并工具是从 X-AnyLabeling 项目移植过来的功能，用于自动合并相邻的文本框区域。这对于处理被分割成多个小块的文本非常有用。

## 如何打开

有两种方式打开区域合并工具：

1. **菜单方式**：点击顶部菜单栏 `工具 (Tools)` -> `区域合并工具 (Region Merge Tool)`
2. **快捷键**：按 `Ctrl+Shift+M`

## 主要功能

### 1. 合并模式

- **垂直合并**：合并上下相邻的文本框
- **水平合并**：合并左右相邻的文本框
- **先垂直后水平**：先进行垂直合并，再进行水平合并
- **先水平后垂直**：先进行水平合并，再进行垂直合并
- **无**：不进行合并

### 2. 文本合并顺序

根据不同的标签类型，可以设置不同的文本合并顺序：

- **从左到右 (LTR)**：适用于英文等从左到右阅读的文本
- **从右到左 (RTL)**：适用于日文气泡等从右到左阅读的文本
- **从上到下 (TTB)**：适用于竖排文本

默认配置：
- RTL 标签：`balloon,qipao,shuqing`
- TTB 标签：`changfangtiao,hengxie`

### 3. 标签合并规则

- **标签合并策略**：
  - 优先使用较短的标签
  - 使用第一个框的标签
  - 组合标签 (label1+label2)
  - 优先使用非默认标签

- **黑名单标签**：可以设置不参与合并的标签（默认：`other`）

- **要求标签完全相同才合并**：只有标签完全相同的文本框才会合并

- **仅在特定标签组内合并**：只在指定的标签组内进行合并

### 4. 几何合并参数

#### 垂直合并（上下）
- **最大垂直间隙**：两个文本框之间的最大垂直距离（像素）
- **最小水平重叠比例**：两个文本框在水平方向上的最小重叠百分比

#### 水平合并（左右）
- **最大水平间隙**：两个文本框之间的最大水平距离（像素）
- **最小垂直重叠比例**：两个文本框在垂直方向上的最小重叠百分比

### 5. 高级选项

- **允许负间隙**：允许文本框本身有重叠的情况下进行合并

### 6. 合并结果类型

- **合并水平矩形**：合并后生成水平矩形
- **合并旋转矩形**：合并后生成旋转矩形（保持原有角度）

## 使用步骤

1. 打开一个包含 JSON 标注文件的项目
2. 点击菜单 `工具` -> `区域合并工具` 或按 `Ctrl+Shift+M`
3. 根据需要调整合并参数
4. 选择执行方式：
   - **对当前文件运行**：只处理当前打开的图片
   - **对所有文件运行**：处理项目中的所有图片
5. 点击相应按钮执行合并
6. 查看合并结果

## 注意事项

1. **备份数据**：合并操作会直接修改 JSON 文件，建议在操作前备份重要数据
2. **参数调整**：不同的图片可能需要不同的参数设置，建议先在单个文件上测试
3. **标签配置**：根据实际使用的标签名称调整 RTL 和 TTB 标签列表
4. **重新加载**：合并完成后，当前页面会自动重新加载以显示最新结果

## 技术细节

### 文件位置

- 合并逻辑核心：`BallonsTranslator/utils/merger.py`
- 对话框界面：`BallonsTranslator/ui/merge_dialog.py`
- 主窗口集成：`BallonsTranslator/ui/mainwindow.py`
- 菜单栏修改：`BallonsTranslator/ui/mainwindowbars.py`

### 合并算法

工具使用并查集（Disjoint Set Union）算法来识别可以合并的文本框组，然后根据配置的参数进行合并。

### 支持的形状类型

- 水平矩形 (rectangle)
- 旋转矩形 (rotation)

## 常见问题

**Q: 为什么有些文本框没有被合并？**
A: 检查以下几点：
- 文本框之间的间隙是否超过了设置的最大间隙
- 文本框的重叠比例是否满足最小重叠比例要求
- 标签是否在黑名单中
- 是否启用了"要求标签完全相同才合并"选项

**Q: 合并后的文本顺序不对怎么办？**
A: 检查文本合并顺序设置，确保标签对应的阅读方向正确。

**Q: 如何撤销合并操作？**
A: 目前没有内置的撤销功能，建议使用版本控制系统（如 Git）或在操作前手动备份 JSON 文件。

## 更新日志

- 2024-11-29：首次集成到 BallonsTranslator 项目
  - 从 X-AnyLabeling 移植核心合并算法
  - 添加菜单项和快捷键支持
  - 集成到主窗口工作流
