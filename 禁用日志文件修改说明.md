# 禁用日志文件自动生成 - 修改说明

## 📅 修改日期
2024年11月29日

---

## 🎯 修改目的

禁用 BallonsTranslator 每次启动时自动生成日志文件的功能，避免 `logs/` 目录下积累大量日志文件（如 `_2025_11_29-14_28_59.log`）。

---

## 📂 修改文件

**文件路径**：`BallonsTranslator/utils/logger.py`

**修改位置**：`setup_logging()` 函数

---

## 🔧 具体修改内容

### 修改前的代码

```python
def setup_logging(logfile_dir: str, max_num_logs=14):

    if not osp.exists(logfile_dir):
        os.makedirs(logfile_dir)
    else:
        old_logs = glob(osp.join(logfile_dir, '*.log'))
        old_logs.sort()
        n_log = len(old_logs)
        if n_log >= max_num_logs:
            to_remove = n_log - max_num_logs + 1
            try:
                for ii in range(to_remove):
                    os.remove(old_logs[ii])
            except Exception as e:
                logger.error(e)

    logfilename = datetime.datetime.now().strftime('_%Y_%m_%d-%H_%M_%S.log')
    logfilep = osp.join(logfile_dir, logfilename)
    fh = logging.FileHandler(logfilep, mode='w', encoding='utf-8')
    fh.setFormatter(
        logging.Formatter(
            ("[%(levelname)s] %(module)s:%(funcName)s:%(lineno)s - %(message)s")
        )
    )
    fh.setLevel(logging.DEBUG)
    logger.addHandler(fh)
```

### 修改后的代码

```python
def setup_logging(logfile_dir: str, max_num_logs=14):
    # 禁用日志文件生成，只输出到控制台
    # 如果需要重新启用日志文件，取消下面代码的注释
    
    # if not osp.exists(logfile_dir):
    #     os.makedirs(logfile_dir)
    # else:
    #     old_logs = glob(osp.join(logfile_dir, '*.log'))
    #     old_logs.sort()
    #     n_log = len(old_logs)
    #     if n_log >= max_num_logs:
    #         to_remove = n_log - max_num_logs + 1
    #         try:
    #             for ii in range(to_remove):
    #                 os.remove(old_logs[ii])
    #         except Exception as e:
    #             logger.error(e)

    # logfilename = datetime.datetime.now().strftime('_%Y_%m_%d-%H_%M_%S.log')
    # logfilep = osp.join(logfile_dir, logfilename)
    # fh = logging.FileHandler(logfilep, mode='w', encoding='utf-8')
    # fh.setFormatter(
    #     logging.Formatter(
    #         ("[%(levelname)s] %(module)s:%(funcName)s:%(lineno)s - %(message)s")
    #     )
    # )
    # fh.setLevel(logging.DEBUG)
    # logger.addHandler(fh)
    
    pass  # 不生成日志文件
```

---

## ✅ 修改效果

### 修改前
- ❌ 每次启动生成新的日志文件（如 `_2025_11_29-14_28_59.log`）
- ❌ 日志文件会不断积累，占用磁盘空间
- ✅ 自动清理超过 14 个的旧日志文件
- ✅ 日志同时输出到文件和控制台

### 修改后
- ✅ **不再生成日志文件**
- ✅ **日志信息仍然输出到控制台**（命令行窗口）
- ✅ **不占用磁盘空间**
- ✅ **保留所有日志功能**（只是不写入文件）

---

## 🔄 如何重新启用日志文件

如果需要重新启用日志文件功能（例如用于调试），按以下步骤操作：

1. 打开文件：`BallonsTranslator/utils/logger.py`
2. 找到 `setup_logging()` 函数
3. 删除所有注释符号 `#`
4. 删除最后的 `pass` 语句
5. 保存文件
6. 重启 BallonsTranslator

---

## 🗑️ 清理现有日志文件

如果想删除已经生成的日志文件：

**方法 1：手动删除**
1. 进入 `BallonsTranslator/logs/` 目录
2. 删除所有 `.log` 文件

**方法 2：命令行删除**
```bash
# Windows CMD
del BallonsTranslator\logs\*.log

# Windows PowerShell
Remove-Item BallonsTranslator\logs\*.log

# Linux/Mac
rm BallonsTranslator/logs/*.log
```

---

## 📊 修改统计

- **修改文件数量**：1 个
- **修改行数**：约 30 行（注释掉）
- **新增行数**：2 行（注释说明 + pass 语句）
- **删除行数**：0 行（保留代码以便恢复）

---

## ⚠️ 注意事项

1. **日志功能仍然正常**：
   - 控制台仍然会显示所有日志信息
   - 日志级别、格式、颜色等功能不受影响

2. **调试时的建议**：
   - 如果遇到问题需要调试，可以临时重新启用日志文件
   - 日志文件对于追踪历史问题很有帮助

3. **不影响其他功能**：
   - 此修改只影响日志文件的生成
   - 不影响程序的任何其他功能

---

## 🔍 技术说明

### 原理
- `setup_logging()` 函数负责配置日志系统
- 原代码会创建 `FileHandler` 将日志写入文件
- 修改后只保留 `StreamHandler`（控制台输出）

### 日志系统架构
```
logger (ColoredLogger)
  ├── StreamHandler (控制台输出) ✅ 保留
  └── FileHandler (文件输出)     ❌ 已禁用
```

---

## 📝 版本信息

**修改版本**：基于原始 BallonsTranslator 项目  
**修改日期**：2024年11月29日  
**功能状态**：✅ 已生效  
**可逆性**：✅ 可以轻松恢复

---

## 📧 相关问题

如有疑问，请参考：
- 原始代码已保留（注释状态），可随时恢复
- 控制台日志仍然可用，不影响调试
- 如需帮助，请查看 `utils/logger.py` 文件中的注释

---

**文档版本**：v1.0  
**最后更新**：2024年11月29日
