# 添加停止功能说明

## 问题描述
点击 RUN 运行之后软件没有停止功能，想停止只能粗暴地关闭软件。运行时进度对话框是模态的，无法操作主界面。

## 解决方案
1. 在图像翻译进度对话框中添加了"停止"按钮
2. 将进度对话框改为非模态，允许用户在运行时操作主界面
3. 点击停止后立即关闭进度对话框，恢复界面操作

## 修改的文件

### 1. BallonsTranslator/ui/custom_widget/message.py
- 在 `ImgtransProgressMessageBox` 类中添加了停止按钮
- 添加了 `stop_clicked` 信号
- 添加了 `on_stop_clicked()` 方法处理停止按钮点击事件
- 在 `zero_progress()` 方法中重置停止按钮状态

主要修改：
```python
class ImgtransProgressMessageBox(ProgressMessageBox):
    stop_clicked = Signal()  # 新增信号
    
    def __init__(self, *args, **kwargs):
        # ... 原有代码 ...
        
        # 添加停止按钮
        self.stop_button = QPushButton(self.tr('Stop'), self)
        self.stop_button.clicked.connect(self.on_stop_clicked)
        # ... 布局代码 ...
    
    def on_stop_clicked(self):
        self.stop_button.setEnabled(False)
        self.stop_button.setText(self.tr('Stopping...'))
        self.stop_clicked.emit()
```

### 2. BallonsTranslator/ui/module_manager.py
- 在 `ImgtransThread` 类中添加了 `stop_requested` 标志
- 添加了 `requestStop()` 方法用于请求停止任务
- 在 `_imgtrans_pipeline()` 方法的循环中添加停止检查
- 在 `ModuleManager` 类中添加了 `stopImgtransPipeline()` 方法
- 连接进度对话框的 `stop_clicked` 信号到停止方法

主要修改：
```python
class ImgtransThread(QThread):
    def __init__(self, ...):
        # ... 原有代码 ...
        self.stop_requested = False  # 新增停止标志
    
    def requestStop(self):
        """请求停止当前任务"""
        self.stop_requested = True
        # 同时停止翻译线程
        if self.translate_thread.isRunning():
            self.translate_thread.imgtrans_proj = None
            self.translate_thread.finished_counter = 0
            self.translate_thread.pipeline_pagekey_queue.clear()
    
    def _imgtrans_pipeline(self):
        # ... 原有代码 ...
        for imgname in self.imgtrans_proj.pages:
            # 检查是否请求停止
            if self.stop_requested:
                LOGGER.info('Image translation pipeline stopped by user')
                break
            # ... 处理图像 ...

class ModuleManager(QObject):
    def setupThread(self, ...):
        # ... 原有代码 ...
        # 连接停止按钮信号
        self.progress_msgbox.stop_clicked.connect(self.stopImgtransPipeline)
    
    def stopImgtransPipeline(self):
        """停止图像翻译流程"""
        LOGGER.info('Stopping image translation pipeline...')
        if self.imgtrans_thread.isRunning():
            self.imgtrans_thread.requestStop()
```

## 功能特点

1. **立即停止**：点击停止按钮后立即终止线程并关闭进度对话框

2. **非模态对话框**：进度对话框改为非模态，运行时可以操作主界面

3. **界面恢复**：停止后界面立即恢复到可操作状态

4. **多线程协调**：同时停止主翻译线程和并行翻译线程

5. **日志记录**：在日志中记录停止操作，便于调试和追踪

## 使用方法

1. 点击 RUN 按钮开始图像翻译任务
2. 进度对话框显示，但不会阻止你操作主界面
3. 如果需要停止任务，点击"Stop"按钮
4. 进度对话框立即关闭，任务停止
5. 界面恢复到可操作状态

## 注意事项

- 停止操作是立即的，会强制终止正在运行的线程
- 当前正在处理的页面可能不会保存
- 已完成的页面数据会被保存
- 下次运行时可以选择"继续运行"来处理剩余的页面
