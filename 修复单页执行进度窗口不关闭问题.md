# 修复单页执行进度窗口不关闭问题

## 问题描述

当使用"本页"按钮执行单页OCR/翻译时，如果同时启用了翻译功能，进度窗口在任务完成后不会自动关闭，即使所有进度条都显示100%。

### 问题表现
- 单独执行检测：✅ 正常关闭
- 单独执行OCR：✅ 正常关闭
- 单独执行翻译：✅ 正常关闭
- 单独执行修复：✅ 正常关闭
- 检测 + OCR + 翻译：❌ 不关闭
- 任何组合 + 翻译：❌ 不关闭

## 问题原因

### 根本原因
在`translate_finished()`方法中，当使用并行翻译模式时，检查的是：
```python
return self.translate_thread.pipeline_finished()
```

而`pipeline_finished()`方法的实现是：
```python
def pipeline_finished(self):
    if self.imgtrans_proj is None:
        return True
    elif self.finished_counter == len(self.imgtrans_proj.pages):
        return True
    return False
```

### 问题分析
- `len(self.imgtrans_proj.pages)` = 项目总页数（例如595页）
- `finished_counter` = 已完成翻译的页数（单页模式下为1）
- 比较：`1 == 595` → `False`
- 结果：`translate_finished()` 永远返回 `False`
- 后果：`proj_finished()` 返回 `False`，窗口不关闭

### 为什么单独翻译可以？
单独翻译时，其他阶段（检测、OCR、修复）都没有启用，它们的`*_finished()`方法直接返回`True`，所以`proj_finished()`可以返回`True`。

但是当翻译和其他阶段一起启用时，`translate_finished()`返回`False`，导致`proj_finished()`返回`False`。

## 解决方案

### 修改文件
`BallonsTranslator/ui/module_manager.py`

### 修改内容
将`translate_finished()`方法中的检查逻辑从：
```python
if self.parallel_trans:
    return self.translate_thread.pipeline_finished()
```

改为：
```python
if self.parallel_trans:
    # 检查翻译计数器是否达到需要处理的页面数
    return self.translate_thread.finished_counter >= self.num_pages
```

### 修改原理
- `self.num_pages` = 需要处理的页面数（单页模式下为1）
- `finished_counter` = 已完成翻译的页数（单页模式下为1）
- 比较：`1 >= 1` → `True`
- 结果：`translate_finished()` 返回 `True`
- 后果：`proj_finished()` 返回 `True`，窗口正常关闭

## 代码对比

### 修改前
```python
def translate_finished(self) -> bool:
    if self.imgtrans_proj is None \
        or not cfg_module.enable_ocr \
        or not cfg_module.enable_translate:
        return True
    if self.parallel_trans:
        return self.translate_thread.pipeline_finished()  # 问题所在
    return self.translate_counter == self.num_pages or not cfg_module.enable_translate
```

### 修改后
```python
def translate_finished(self) -> bool:
    if self.imgtrans_proj is None \
        or not cfg_module.enable_ocr \
        or not cfg_module.enable_translate:
        return True
    if self.parallel_trans:
        # 检查翻译计数器是否达到需要处理的页面数
        return self.translate_thread.finished_counter >= self.num_pages  # 修复
    return self.translate_counter == self.num_pages or not cfg_module.enable_translate
```

## 影响范围

### 修复的场景
- ✅ 单页执行：检测 + 翻译
- ✅ 单页执行：OCR + 翻译
- ✅ 单页执行：检测 + OCR + 翻译
- ✅ 单页执行：检测 + OCR + 翻译 + 修复
- ✅ 继续运行模式下的翻译

### 不受影响的场景
- ✅ 全部运行（所有页面）
- ✅ 单独执行各个阶段
- ✅ 停止功能

## 测试验证

### 测试步骤
1. 打开包含多张图片的项目
2. 选择任意一张图片
3. 启用检测、OCR、翻译（可选修复）
4. 点击"本页"按钮
5. 观察进度窗口是否在完成后自动关闭

### 预期结果
- ✅ 所有进度条显示100%
- ✅ 进度窗口自动关闭
- ✅ 界面恢复可操作状态

## 总结

这是一个典型的"边界条件"问题。原有代码在设计时只考虑了处理所有页面的情况，没有考虑到单页模式下`num_pages`会变成1的情况。

修复方法是使用正确的比较基准：用`self.num_pages`（需要处理的页面数）代替`len(self.imgtrans_proj.pages)`（项目总页数）。
