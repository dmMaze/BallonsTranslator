# 添加"继续运行"功能 - 修改说明

## 📅 修改日期
2024年11月29日

---

## 🎯 功能描述

在运行 OCR/翻译时，如果项目中已经有翻译结果，现在提供三个选项：
1. **重新运行** - 清空所有结果，从头开始
2. **继续运行** - 跳过已处理的内容，只处理未完成的部分
3. **取消** - 不执行任何操作

---

## 📂 修改文件

**文件路径**：`BallonsTranslator/ui/mainwindow.py`

**修改方法**：
- `run_imgtrans()` - 添加三按钮选择对话框
- `on_run_imgtrans(continue_mode=False)` - 添加继续模式支持

---

## 🔧 具体修改内容

### 1. 修改 `run_imgtrans()` 方法

#### 修改前
```python
def run_imgtrans(self):
    if not self.imgtrans_proj.is_all_pages_no_text and not pcfg.module.keep_exist_textlines:
        reply = QMessageBox.question(self, self.tr('Confirmation'),
                                     self.tr('Are you sure to run image translation again?\nAll existing translation results will be cleared!'),
                                     QMessageBox.Yes | QMessageBox.No, QMessageBox.No)
        if reply != QMessageBox.Yes:
            return
    self.on_run_imgtrans()
```

#### 修改后
```python
def run_imgtrans(self):
    if not self.imgtrans_proj.is_all_pages_no_text and not pcfg.module.keep_exist_textlines:
        # 创建自定义消息框，添加"继续运行"选项
        msgBox = QMessageBox(self)
        msgBox.setIcon(QMessageBox.Question)
        msgBox.setWindowTitle(self.tr('Confirmation'))
        msgBox.setText(self.tr('Are you sure to run image translation again?\nAll existing translation results will be cleared!'))
        
        # 添加三个按钮
        restart_btn = msgBox.addButton(self.tr('Restart'), QMessageBox.YesRole)  # 重新运行
        continue_btn = msgBox.addButton(self.tr('Continue'), QMessageBox.AcceptRole)  # 继续运行
        cancel_btn = msgBox.addButton(self.tr('Cancel'), QMessageBox.RejectRole)  # 取消
        
        msgBox.setDefaultButton(continue_btn)
        msgBox.exec_()
        
        clicked_button = msgBox.clickedButton()
        if clicked_button == cancel_btn:
            return  # 取消，不执行任何操作
        elif clicked_button == continue_btn:
            # 继续运行：只处理没有文本的页面
            self.on_run_imgtrans(continue_mode=True)
            return
        # 如果是 restart_btn，继续执行下面的代码（重新运行）
    self.on_run_imgtrans()
```

### 2. 修改 `on_run_imgtrans()` 方法

#### 修改前
```python
def on_run_imgtrans(self):
    # ... 省略部分代码 ...
    if pcfg.module.enable_detect:
        for page in self.imgtrans_proj.pages:
            if not pcfg.module.keep_exist_textlines:
                self.imgtrans_proj.pages[page].clear()
    else:
        # ... 省略部分代码 ...
        for textblk in blklist:
            # ... 省略部分代码 ...
            if pcfg.module.enable_ocr:
                textblk.text = []
                textblk.set_font_colors((0, 0, 0), (0, 0, 0))
```

#### 修改后
```python
def on_run_imgtrans(self, continue_mode=False):
    # ... 省略部分代码 ...
    if pcfg.module.enable_detect:
        for page in self.imgtrans_proj.pages:
            if not pcfg.module.keep_exist_textlines:
                # 继续模式：只清空没有文本的页面
                if continue_mode:
                    if len(self.imgtrans_proj.pages[page]) == 0:
                        self.imgtrans_proj.pages[page].clear()
                else:
                    self.imgtrans_proj.pages[page].clear()
    else:
        # ... 省略部分代码 ...
        for textblk in blklist:
            # ... 省略部分代码 ...
            # 继续模式：跳过已有文本的文本块
            if continue_mode and textblk.text:
                continue
            if pcfg.module.enable_ocr:
                textblk.text = []
                textblk.set_font_colors((0, 0, 0), (0, 0, 0))
```

---

## ✅ 功能效果

### 使用场景

**场景 1：OCR 中断后继续**
- 正在处理 100 张图片的 OCR
- 处理到第 50 张时程序崩溃或手动停止
- 重新启动后，选择"继续运行"
- ✅ 程序会跳过前 50 张，从第 51 张开始处理

**场景 2：部分图片需要重新处理**
- 已经完成了所有图片的 OCR
- 手动删除了某些图片的文本框
- 点击运行，选择"继续运行"
- ✅ 程序只处理被删除文本的图片

**场景 3：完全重新开始**
- 对结果不满意，想重新处理
- 点击运行，选择"重新运行"
- ✅ 清空所有结果，从头开始

### 三个按钮的区别

| 按钮 | 英文 | 功能 | 适用场景 |
|------|------|------|----------|
| 重新运行 | Restart | 清空所有结果，从头开始 | 对结果不满意，想重新处理 |
| 继续运行 | Continue | 跳过已处理的内容，只处理未完成的 | OCR 中断后继续，或部分重新处理 |
| 取消 | Cancel | 不执行任何操作 | 误点击，不想运行 |

---

## 🔍 技术实现

### 继续模式的判断逻辑

**检测模式（enable_detect）**：
```python
if continue_mode:
    if len(self.imgtrans_proj.pages[page]) == 0:
        # 只处理没有文本块的页面
        self.imgtrans_proj.pages[page].clear()
```

**OCR/翻译模式**：
```python
if continue_mode and textblk.text:
    continue  # 跳过已有文本的文本块
```

### 默认按钮
- 默认选中"继续运行"按钮
- 用户可以直接按回车键选择继续运行
- 更符合用户的常见需求（中断后继续）

---

## 📊 修改统计

- **修改文件数量**：1 个
- **修改方法数量**：2 个
- **新增代码行数**：约 20 行
- **修改代码行数**：约 15 行

---

## ⚠️ 注意事项

1. **继续模式的判断依据**：
   - 检测模式：根据页面是否有文本块判断
   - OCR 模式：根据文本块是否有文本内容判断

2. **不影响原有功能**：
   - 如果项目中没有任何翻译结果，不会弹出对话框
   - 如果启用了"保留现有文本行"选项，也不会弹出对话框

3. **适用范围**：
   - 适用于 OCR
   - 适用于文本检测
   - 适用于翻译
   - 适用于所有图片翻译流程

---

## 🎨 界面展示

### 对话框按钮布局
```
┌─────────────────────────────────────┐
│  确认                          [X]  │
├─────────────────────────────────────┤
│  确定要重新运行吗？                 │
│  现有翻译结果将被清空！             │
│                                     │
│  [重新运行] [继续运行] [取消]      │
└─────────────────────────────────────┘
```

### 按钮说明
- **重新运行**：清空所有结果，从第一张图片开始
- **继续运行**（默认）：跳过已处理的图片，从中断处继续
- **取消**：关闭对话框，不执行任何操作

---

## 🔄 使用流程

### 正常使用流程
1. 打开项目
2. 点击"运行"按钮
3. 如果有现有结果，弹出对话框
4. 选择操作：
   - 点击"继续运行"：从中断处继续
   - 点击"重新运行"：清空重新开始
   - 点击"取消"：不执行

### 中断后继续流程
1. OCR 处理到一半时程序崩溃/手动停止
2. 重新启动程序
3. 打开同一个项目
4. 点击"运行"按钮
5. 选择"继续运行"
6. ✅ 程序自动跳过已处理的图片，继续处理剩余图片

---

## 🐛 已知问题

暂无

---

## 📝 版本信息

**修改版本**：基于原始 BallonsTranslator 项目  
**修改日期**：2024年11月29日  
**功能状态**：✅ 已完成  
**测试状态**：⏳ 待测试

---

## 📧 相关问题

### Q1: 如何判断哪些图片已经处理过？
**A**: 
- 检测模式：根据页面是否有文本块（`len(pages[page]) > 0`）
- OCR 模式：根据文本块是否有文本内容（`textblk.text` 不为空）

### Q2: 继续运行会重新处理已完成的图片吗？
**A**: 不会。继续运行会跳过所有已有文本的图片和文本块。

### Q3: 如果我想重新处理某几张图片怎么办？
**A**: 
1. 手动删除那几张图片的文本框
2. 点击运行，选择"继续运行"
3. 程序会只处理被删除文本的图片

### Q4: 默认选择哪个按钮？
**A**: 默认选择"继续运行"，因为这是最常见的需求（中断后继续）。

---

**文档版本**：v1.0  
**最后更新**：2024年11月29日
